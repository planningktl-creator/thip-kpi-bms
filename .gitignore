node_modules/
dist/
.vite/
coverage/
tmp/
.env
.env.*
!.env.example
*.local
*.log

# Invalid aggregate fixtures are regenerated by `pnpm fixture:export` for CI.
test-fixtures/invalid/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
